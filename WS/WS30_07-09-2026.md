# WS30 · 07/09/2026 · Bloque C (Comunidad) — front (C2) y cierre

Contexto al abrir: la API del Bloque C está completa y con Q/A (WS29, suite 751 ✓);
prod en `dwellia-api-00018` (Bloque B, **prueba real de Tomás hecha**); la rama
`epic/v2-bloque-c-comunidad` limpia e igual a origin. Plan exacto: `WS29` §7.

## 0. Preparación (orquestador)
- `main.tsx`: rutas `/baul/recomendacion/nueva` y `/baul/recomendacion/:id/editar`
  con `screens/Recomendacion.tsx` como lugar reservado (commit `5b770a3`). Ningún
  agente toca `main.tsx`.
- API local en modo admin (`dev|user`, `demo|a22`), nohup en :8000 · `make demo-seed`
  idempotente (republicó 3 cartas del mazo que la suite había borrado) · Vite en :8081.
- Decisiones de Tomás: **Opus + Opus** para C2.1/C2.2 · máx. 2 agentes en simultáneo.

## 1. Ola C2 — C2.1 (Comunidad) + C2.2 (Baúl y Perfil), en paralelo · ✅
Dos Opus en paralelo, ~9 y ~11 min, 189k + 214k tokens, **cero colisiones** (cada
uno tocó exactamente su territorio; CSS propio `comunidad.css` / `baul.css`).
- **C2.1** (`cc12b25`): `/comunidad` (buscador con debounce que REEMPLAZA la
  pantalla · Te enviaron · Solicitudes · Tu comunidad · Descubrir en grilla de 2) ·
  `CtaVinculo` único por estado · `/comunidad/:usuarioId` · `/comunidad/ficha/:id`
  (guardar/quitar, hacer la Pausa ahora o como próxima, reenviar) · `/pausa/:id` ·
  `ReenviarSheet` (comunidad + WhatsApp) · `Reflect` carga la entrega de la ruta ·
  `Avisos` rutea `reenvio`/`solicitud`. Supuestos del agente: la ficha ajena usa
  `api.fichaAjena` (no `entrega`, que es solo del dueño) · "siguiente" nunca da 409
  (reemplaza la programada) · toda fila del buscador lleva al perfil.
- **C2.2** (`6114cb9`): Baúl con `baulCompleto` (Pausa · Pausa de <apodo> ·
  Recomendación) · chips **Recomendaciones** y **De mi comunidad** · wizard de 2
  pasos (`Llevas N de 30`, enlace https validado, `url ''` borra al editar) · detalle
  de guardada (Ficha ajena) y de recomendación · la propia suma Reenviar (link de
  regalo reusado de `sessionStorage share:{id}`) · Perfil: avatar + foto (JPG/PNG/
  WebP ≤8 MB) + toggle Perfil público · AccionesArriba con la foto.
- Huecos cerrados por el orquestador: `api.setPerfil` declara `perfil_publico`
  (`c4ec287`). Anotado, sin tocar: `Ficha` no acepta `acciones` en modo propia
  (EntryDetail envuelve la ficha + Reenviar en `.ficha-con-reenviar`) · los topes
  80/500/30 viven en `screens/Recomendacion.tsx`, no en `lib/` · no hay CTA para
  **quitar** a alguien de mi comunidad (la API lo tiene).

## 2. Contrato en navegador (orquestador, `demo|a22`, por JS: el panel oculto vence los clics)
Todo con **cero errores** de ventana ni de promesas: buscar "mar" → Aceptar → Mar en
Tu comunidad y Solicitudes desaparece · Te enviaron → ficha de Lu → Guardar ⇄ Quitar
→ Hacer la Pausa › Hacer ahora → `/pausa/:id` (Card de frente, "Ver mi pausa" gira)
→ Ya la hice → `/reflexionar/:id` **con la carta de Lu** · "Que sea mi próxima carta"
→ confirmación inline · Reenviar → panel con Lu y Mar + WhatsApp → "Enviada a Lu." ·
Baúl con los 3 tipos de fila · wizard: enlace sin https rebota con mensaje, guardar →
fila nueva "con enlace" · detalle → visibilidad → Editar (título nuevo, enlace
vacío = borrado) → Borrar con confirmación · Perfil público persiste al volver ·
foto por canvas → "Cambiar foto"/"Quitar" · 9 MB → "La foto no puede pesar más de
8 MB." · Quitar OK.
Observaciones menores (para el Q/A visual): `Reflect` sobre una Pausa extra dice
"Tu Pausa de hoy" · el backend deja reenviar a Lu su propia Pausa.
Estado del seed restaurado después (borré el vínculo a22↔Mar y `make demo-seed`
recreó la solicitud pendiente).

## 3. Q/A visual de Tomás
- Pop-up `SoloComunidad`: había CINCO textos distintos para la misma puerta. Ahora
  UN texto en el componente (`9df4750`): "Esta función es para miembros de la
  Comunidad Dwellia" · "Si quieres disfrutar todas las funciones de Dwellia, es tan
  simple como formar parte de su Comunidad".
- Q/A de Tomás, ronda 1 (decisiones): "Tu Pausa de hoy" en la extra se QUEDA ·
  **quitar a alguien de tu comunidad** con pop-up y CTA rojo (todo destructivo en rojo) ·
  **reenviar CON comentario** (≤200, opcional; llega con la ficha) · perfil ajeno en
  **dos pestañas** Pausas / Recomendaciones · **pantalla propia** para la recomendación
  ajena con el enlace como botón según el dominio · **fotos en recomendaciones → POST_MVP
  P7** · **Crear = la única puerta**: "Escribir una carta" / "Recomendar algo" (ambas
  premium); el CTA del Baúl se retira.
- Backend del comentario (`554a74b`): migración `n4c5d6e7f8a9` (ida y vuelta) ·
  `reenvios.comentario` · `_comentario_limpio` (recorte, vacío = null, largo = 422 SIN
  rastro) · `recibidos` lo devuelve · seed lo completa en los reenvíos ya sembrados ·
  5 tests · contrato del cliente · ruta `/comunidad/:usuarioId/recomendacion/:id`.
- Ola **C2b** (1 Opus): los 6 puntos del front.
- **C2b construida** (`d803819`, 1 Opus, 173k tokens, 12 archivos): los 6 puntos.
  Supuestos del agente: el free también ve las dos tarjetas de Crear (abren el pop-up) ·
  subtítulo de Crear "Deja algo para la comunidad" · el comentario NO viaja por WhatsApp
  (solo el link) · "Rechazar"/"Cancelar solicitud" en rojo con `.com-btn.es-no` (tamaño de
  fila) · no hay endpoint por id para recomendación ajena ni "el reenvío de esta ficha":
  se filtra en el front sobre `baulDe` / `reenviosRecibidos` (anotar en POST_MVP si crece).
- Contrato en navegador (orquestador, `demo|a22`, cero errores): "Te enviaron" con el
  comentario «…» · la ficha muestra "Lu te la envió: «…»" · perfil de Lu: Pausas (3) /
  Recomendaciones (2) + "Quitar de mi comunidad" → pop-up con "Sí, quitar" en rojo ·
  la recomendación de Lu en su pantalla · Crear con "Escribir una carta" / "Recomendar algo".
- **Sigue: Q/A visual de Tomás ronda 2 → C3 (T&C v3 + fotos huérfanas) → E2E → deploy.**

## 4. C3 — legal y cierre (orquestador, sin agente) · ✅ `bcabede`
- **Términos v3** (`Terms.tsx`, 7/9/2026): comunidad y perfil público (privado por
  defecto; te encuentran, ven quienes aceptaste) · las pausas nacen privadas, compartirlas
  es decisión del usuario y es reversible (desaparece también de quien la guardó) · lo
  que escribe para la comunidad: cesión de las cartas aprobadas (quedan sin nombre aunque
  borre la cuenta), moderación con aviso, recomendaciones = opiniones personales,
  enlaces externos con sus propias reglas · Stripe gestiona el pago, no vemos la
  tarjeta · borrar la cuenta borra lo compartido y las recomendaciones · +16 y respeto.
  Solo texto y fecha, sin re-aceptación (decisión WS29).
- **Fotos huérfanas (deuda T7 cerrada):** `services/limpieza.py` = fotos de entregas
  NO completadas con más de 7 días (archivo primero, fila después; la entrega no se
  toca) · corre dentro del barrido de avisos (`/api/internal/aviso-diario`, cada 15')
  sin poder tumbarlo · `make limpiar-fotos` (ensayo) / `ARGS='--borrar'` · 2 tests.
  En la base local: 0 huérfanas.
- Tomás dio por cubierto el E2E con el contrato en navegador + su Q/A visual; la
  compuerta pre-merge es la suite completa del backend.

## 5. Deploy
Compuerta: suite completa **758 ✓** (exit 0). Orden que importa (el back antes del push):
1. `gcloud builds submit --tag europe-west1-docker.pkg.dev/dwellia-app/dwellia/api:latest --project dwellia-app .` (raíz; 1m07s, sha256:8a3c2c9e…).
2. `gcloud run jobs execute dwellia-migrate --wait` → `l2a3… → m3b4c5d6e7f8 → n4c5d6e7f8a9` aplicadas · "Seed OK · 6 categorías · 4 acciones · 77 cartas".
3. `gcloud run deploy dwellia-api` → **rev `dwellia-api-00019`** al 100 % · `/health` 200 · `/api/reenvios/recibidos` y `/api/fichas/descubrir` sin login → 401 · el rewrite del Hosting llega a la API nueva (401).
4. Merge commit **`c9896ba`** `epic/v2-bloque-c-comunidad → main` · push · CI "Deploy Hosting" verde (17:42Z) · el bundle de prod trae "Recomendar algo", "Quitar de mi comunidad" y los Términos del 7/9.

🌅 **BLOQUE C EN PRODUCCIÓN → la v2 (A+B+C) está COMPLETA.**

## 6. Próxima sesión
- Prueba real de Tomás en prod con su cuenta (admin = premium): comunidad, reenvío con comentario, recomendaciones con enlace, foto de perfil, perfil público.
- **Bloque D** (promoción en TikTok · el libro animado): `Flipbook/` + roadmap §5b.
- Deudas nuevas en POST_MVP (P7 fotos en recomendaciones) y en §1 de esta bitácora.
