# WS28 · 07/09/2026 — Bloque B (Crear): B2.2 front CONSTRUIDA · contrato validado en navegador

Branch `epic/v2-bloque-b-crear`. Arranque: backend del bloque completo (633 tests), WIP del front `5962527` sin cablear ni CSS.

## 1. Decisiones de Tomás (esta sesión)
- **Sin Q/A visual por agentes.** Claude valida el contrato backend↔front (llamadas, estados, guardas) en el navegador embebido; Tomás hace el Q/A visual en local. Primero un Q/A simple de estética (WS28) y en la WS29 el E2E completo con casos construidos en la base.
- **Crear es solo premium**: free ve el porqué + pop-up "Quiero ser parte" y nunca un formulario (`/crear/nueva` y `/crear/:id/editar` redirigen a `/crear`). Free tampoco carga recomendaciones (Bloque C).
- **El premium ve cuánta gente recibió su carta Y el puntaje promedio en estrellas.**

## 2. Contrato (orquestador · `3e7ed2f`)
- `GET /api/cartas-comunidad/mias` suma `estrellas_promedio` (1 decimal, `null` si nadie puntuó) y `veces_puntuada` (entregas de la carta publicada con estrellas). `services/cartas_comunidad.estrellas_de_la_carta`. 2 tests.
- `demo-seed` siembra el **impacto** de la carta cargada: 3 lectores demo con estrellas 5, 4 y una sin puntuar → "3 personas la recibieron · ★ 4,5 · 2 valoraciones" (`453ac6a`). El `conftest` borra primero esas entregas antes de limpiar el mazo (la FK frenaba el borrado, `a843c61`).

## 3. Ola front (2 agentes Opus en paralelo, territorios disjuntos · `8a0362d`, 16 archivos, +2.751 líneas)
- **B2.2a Crear + Avisos:** rutas `/crear`, `/crear/nueva`, `/crear/:id/editar`, `/avisos`, `/admin` (+ `RequireAdmin`) · `screens/crear.css` con todo el CSS del WIP · impacto + puntaje (`textoPuntaje`) · `components/Campana.tsx` en Hoy y Crear (contador, recarga al volver la pestaña) · `screens/Avisos.tsx` · Perfil › "Escritorio de Dwellia" solo con `es_admin` · copy de Premium: escribir cartas es de quienes son parte.
- **B2.2b Adminland:** `screens/Admin.tsx` + `admin/{Cola,Termometro,Comentarios,comun}.tsx` + `admin.css` (`.frame-inner.is-wide` 960 px). Cola con filtros (Pendientes · Con retoque · Cargadas · No aprobadas), funnel v1 (historial) / v2 (juez: rótulos, hallazgos, fix, concepto) / vFinal (Aprobar con concepto opcional y confirmación inline · No aprobar con motivo · Pedir un retoque con sugerencia + fix opcional). Termómetro (4 tarjetas + barras por pilar Dwellia/comunidad). Comentarios agrupados por carta, peor primero, chip "Solo 1-2 estrellas". 403 → "Esta pantalla es solo para Dwellia."
- tsc ✓ con todo junto.

## 4. Validación del contrato en navegador (orquestador, `demo|a22` admin+premium, uvicorn con `MINDFUL_ADMIN_UIDS`)
Cero errores de consola en todo el recorrido. Llamadas y estados verificados:
- `/crear`: lista con los 4 estados, carta cargada muestra "3 personas la recibieron · ★ 4,5 · 2 valoraciones", CTA apagado mientras hay una en curso (`a_revisar` cuenta como en curso, igual que el backend).
- `/avisos`: 3 avisos, 1 sin leer, campana con "1".
- `/admin`: cola 200 · **aprobar** (POST 200, cola 7→6, mazo comunidad +1) · **no aprobar** con motivo (200) · **pedir retoque** con sugerencia (200) · termómetro (resumen 200; "1414 · 0" en el texto plano es solo la concatenación total+desglose, el DOM está bien) · comentarios agrupados (200).
- **Reenvío desde "Necesita un retoque"** con `?usar=sugerencia`: wizard precargado (Resiliencia · Respirar · frase sugerida) → PUT 200 → "En proceso de evaluación" (juez apagado en local → `revision_dwellia`); en admin el historial muestra v1/v2/v3; aprobada.
- **Retirar** (DELETE 204) · **wizard nuevo completo** (6 pilares, 4 acciones, previa en vivo, contadores 45/60 y 153/220, firma con apodo, cesión) → POST 201 → aparece primera "En proceso de evaluación".
- **Free** (`make free-demo`): `/crear` = pop-up, sin formulario ni lista; `/crear/nueva` → `/crear`; API: POST 403. **No admin** (`demo|a22b`): `/admin` → `/perfil`; API 403.

## 5. Notas / deudas chicas
- En el funnel v1 del admin la acción se muestra desde el slug (no hay catálogo de acciones en el store). Menor.
- El "Concepto" de Aprobar se precarga con `concepto` de la propuesta; en las sembradas es la marca `demo-seed-…` (artefacto del seed, no pasa en runtime).
- `/avisos` conserva la tab bar (se lee y se vuelve, como Premium).
- Local: `make api-dev-admin` levanta la API con `dev|user` y `demo|a22` como admin. En el navegador: `sessionStorage.setItem('dwellia-demo-sub','demo|a22')`. `make premium-demo` / `make free-demo` conmutan el plan; `make demo-seed` repone (idempotente, republica la carta cargada y su impacto si los tests la borraron).

## 5b. Del Q/A visual de Tomás, ya resuelto (commit `734f9c6`)
- **Límites nuevos: frase ≤40 · prompt 100-150** (antes 60 / 220). Única fuente `services/canon.py` (`MAX_FRASE_COMUNIDAD` …); `cartas_comunidad.py` los reexporta y de ahí router, admin, juez y el front (`propuestas.ts`). Mínimo 100 sin cambios (ventana 100-150 angosta: a evaluar bajar a 80). Seed y filas ya sembradas acortadas. Tests: 37 ajustes en 9 archivos por un agente, sin debilitar ninguno; 4 reescritos porque perdían sentido con 40 (documentados en su informe).
- **El "Necesita un retoque · no combinan" NO es el modelo:** es la capa 1 del canon (matriz pilar × acción, R8.1), determinística, corre siempre. Solución: `GET /api/contenido/matriz` y el paso 2 del wizard muestra SOLO las acciones que combinan con el pilar (nota "Estas son las que combinan con Gratitud"); verificado por pilar en navegador. Test `test_b22_matriz.py`.
- La limpieza propia de `test_qa_b21_adversarial.py` borra antes las entregas de cartas comunidad (mismo FK que el conftest). Suite completa ✓.

## 5c. Segunda ronda del Q/A de Tomás (commit `8dcf7ea`) y decisiones
- **Límites definitivos: frase 20-40 · prompt 80-150** (única fuente `services/canon.py`; router, admin, juez, wizard y Cola con mínimo de frase y contador "faltan N"). Tests de bordes 19/20 y 79/80.
- **La cuenta admin es premium siempre, sin Stripe** (`services/plan.es_premium` mira `MINDFUL_ADMIN_UIDS` por request). En prod ya está el uid Google de tomaszanchetti@gmail.com → ve y usa todo lo de la comunidad. ⚠️ Perfil › Tu plan le mostrará "premium" y el botón del portal de Stripe, que para una cuenta sin customer no abre nada útil (menor).
- **Perfil: CTA "Adminland" abajo de todo**, solo si `es_admin`; se quitó la sección "Escritorio de Dwellia".
- **Decisión de Tomás:** el recorrido de esta sesión (contrato validado en navegador + su Q/A visual del admin "perfecto") **cuenta como el E2E del Bloque B**. El E2E COMPLETÍSIMO se hace al cerrar el Bloque C (fin del desarrollo). Roadmap §2 alineado.

## 6. Deploy a producción (07/09, tarde)
- `gcloud builds submit` (1m17s, imagen `api:latest` sha256:a8f0c616…) → job `dwellia-migrate` (`j0e1f2a3b4c5 → k1f2a3b4c5d6 → l2a3b4c5d6e7` aplicadas · "Seed OK · 6 categorías · 4 acciones · 77 cartas") → `gcloud run deploy dwellia-api` → **rev `dwellia-api-00018`** al 100 %.
- Smoke: `/health` 200 · `/api/contenido/matriz` 200 (directo y por Hosting) · `/api/admin/resumen` y `/api/cartas-comunidad/mias` sin token → 401.
- Merge `epic/v2-bloque-b-crear` → `main` con merge commit → push → CI "Deploy Hosting" (ver §7).

## 7. Próxima sesión (WS29)
1. **Prueba real de Tomás en prod** (cuenta Google = admin = premium): escribir una carta, verla en la cola, aprobarla desde Adminland, ver el aviso y "Cargado a la comunidad".
2. **Bloque C — Comunidad** según `ROADMAP_v2_PREMIUM.md` §5 (C0 contrato → C1 API → C2 front → C3 T&C v3), con `make demo-seed` creciendo (vínculos, reenvíos).
3. Al cerrar C: **E2E completísimo** en local con casos construidos en la base, y recién después el Bloque D (TikTok).

1. Tomás: Q/A visual de estética en local (Crear · wizard · Avisos · Perfil · Admin, como premium, free y admin) → lista de retoques.
2. **E2E del bloque** con casos construidos en la base por Claude (usuarios premium/free, cartas en todos los estados, juez real con la key una vez, carta de la comunidad que le llega a otro usuario como carta del día, aviso + push).
3. Deploy en orden: `gcloud builds submit` → job `dwellia-migrate` (`k1f2a3b4c5d6` + `l2a3b4c5d6e7`) → `gcloud run deploy dwellia-api` → merge `epic/v2-bloque-b-crear` → `main` (merge commit) → push → CI Hosting. Prod ya tiene `MINDFUL_ANTHROPIC_API_KEY` y `MINDFUL_ADMIN_UIDS`.
