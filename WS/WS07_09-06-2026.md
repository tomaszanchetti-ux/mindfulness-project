# WS07 — 09/06/2026 · Stack confirmado vs Arc One + arranque del build (N4 paso 1)

Séptima sesión. Dos cosas: (1) **confirmar el stack/arquitectura/infra** contra el repo
real de Arc One (no de memoria) y dejarlo escrito; (2) **arrancar a construir** — primer
slice del backend, verificado contra Postgres real.

## 1. Stack confirmado — doc nuevo `01_Stack_Arquitectura_Infraestructura.md`

Se leyó `arc-one-sandbox` (CLAUDE.md, requirements, docker-compose, firebase.json,
infra/terraform/*) y `prode-mundial` (auth + notificaciones). Resultado:

- **9 de 11 piezas son calco directo de Arc One** (en producción): FastAPI/Cloud Run ·
  Postgres 16 · Firebase Auth · Cloud Storage · Redis+RQ · Firebase Hosting · Terraform/GCP ·
  Secret Manager · Artifact Registry. Versiones exactas en el doc.
- **2 divergencias, ambas esperadas:**
  - 🟡 **Front: React Native + Expo** (móvil) en vez de Vite (web). Misma familia React+TS.
    PWA-first desde el mismo Firebase Hosting (decisión WS03).
  - 🆕 **Email del aviso diario** — pieza nueva, NO bloqueante (decisión diferida a M2).
- **Hallazgo de arquitectura:** el aviso diario a +100k es lo único que Arc One no resuelve.
  Patrón: **Cloud Scheduler (cron c/15min) → Redis+RQ → worker corre `elegir_carta()`** por
  ventana horaria/TZ. Detalle en §3.2 del doc.

### Corrección sobre el email (pregunta de Tomás: "¿Firebase no sirve? ¿qué usa prode?")

Verificado en el repo de prode:
- **Login magic-link → lo manda Firebase Auth solo** (`sendSignInLinkToEmail`). Gratis,
  built-in. **Cero proveedor.** Es lo que hace prode.
- **Notificaciones → FCM push** (prode no usa email transaccional para nada).
- El "email branded dominio propio" (aprendizaje prode) es real pero menor: el magic-link de
  Firebase sale de `firebaseapp.com` (feo / spam) → para branded hay que configurar dominio.
  **No bloquea.**
- **Único caso que podría pedir proveedor externo:** el aviso diario por email a quien NO
  instaló la PWA (camino B: Resend/etc · camino A: solo FCM como prode). **Decisión → M2.**

## 2. Build N4 · Paso 1 — Seed + esquema ✅ (verificado contra Postgres real)

Se montó el backend en `apps/api/` (espejo de `arc-one-sandbox/apps/api`):

- **`docker-compose.yml`** (Postgres 16) + **`Makefile`** (`db-up`/`api-setup`/`api-migrate`/
  `api-seed`/`api-dev`/`api-test`) + **`.env.example`**.
- **`mindful_api/`**: `config.py` (pydantic-settings) · `db/base.py` (engine+sesión) ·
  `db/models.py` · `seed.py` · `main.py`.
- **`db/models.py` codifica los DOS MUNDOS** — 8 tablas:
  - Global (sin user_id): `categorias` · `acciones` · `cartas`.
  - Privado (todo con `usuario_id`): `usuarios` · `usuario_categorias` · `entregas` ·
    `fotos` · `compartidos`.
- **Alembic** configurado; migración inicial autogenerada (las 8 tablas + índices por `usuario_id`).
- **`seed.py`** carga el contenido global **desde los JSON maestros de M0** (única fuente de
  verdad), idempotente (merge por PK; flush de padres antes de cartas por las FK).

### Verificación (Postgres real en docker, no simulado)

```
make api-migrate → 8 tablas creadas
make api-seed    → Seed OK · 6 categorías · 5 acciones · 69 cartas  (idempotente)
GET /health                  → {"status":"ok"}
GET /api/contenido/resumen   → {"cartas":69}
GET /api/contenido/categorias→ las 6 con colores + assets
pytest                       → 2 passed
```

## 3. Build N4 · Paso 1b — Auth + Perfil (M1) ✅ (verificado: tests + curl en vivo)

Se sumó el "Mundo 2" (datos del usuario). Sin GCP todavía — todo en modo `dev`.

- **`auth.py` · `get_current_user`** (dos modos): `dev` simula el usuario con header
  `X-Debug-Sub`/`X-Debug-Email` (default `dev|user`) · `firebase` valida el `Bearer` ID
  token (lazy-init de firebase-admin, ADC en Cloud Run). **El aislamiento empieza acá:** cada
  request resuelve UN usuario (auto-provisión en el primer login) y la API filtra por su id.
- **`schemas.py`** (Pydantic): `PerfilOut` · `PerfilUpdate` (valida `hora_aviso` HH:MM) ·
  `CategoriasUpdate` (regla M1 **2-6**, sin duplicados).
- **`routers/perfil.py`** (M1): `GET /api/perfil` (perfil + categorías + `onboarding_completo`
  = términos aceptados + ≥2 categorías) · `PUT /api/perfil` (horario/TZ/aviso + aceptar
  términos) · `PUT /api/perfil/categorias` (reemplazo total, valida slugs contra el Mundo 1).
- **Tests** (`test_perfil.py`): flujo onboarding completo · validaciones (422) · **aislamiento
  entre 2 usuarios**. Suite total = **5 passed**. Demo curl: usuario fresco → elegir categorías
  → aceptar términos → `onboarding_completo: true`.

### Decisión: GCP NO hace falta todavía
Todo el backend (auth incluido) se construye y testea en `dev` mode sin nube. El proyecto
**Firebase/GCP** se crea recién cuando (1) querramos probar el login real (magic-link) o
(2) despleguemos. Hasta entonces, el camino `firebase` queda escrito pero inactivo.

## Decisiones / notas

- **Python local = 3.9** (prod target 3.12). Se usó `Optional[...]` (no `X | None`) en modelos
  Y en firmas de parámetros de FastAPI/auth — FastAPI evalúa las anotaciones en runtime y PEP604
  rompe en 3.9.
- **Naming:** paquete `mindful_api`, prefijo de env `MINDFUL_`, DB `mindful`. Identidad dev = `dev|user`.

## Próximo (WS08 o seguir acá)

1. **Paso 2 — Entrega + Ritual:** llevar `elegir_carta()` (ya en `M2_Entrega_del_Dia/`) al
   backend (lee `cartas` global + `entregas` por user_id) + endpoint "carta del día" + cierre M3
   (reflexión/estrellas/fotos). El worker RQ + Cloud Scheduler vienen con el aviso.
2. Pendiente del plan WS06: doc UX/UI general `Madre_del_Diseno.md` (espejo design-system Arc One).
3. Cuando querás login real: crear proyecto Firebase (te guío) + `MINDFUL_AUTH_MODE=firebase`.
