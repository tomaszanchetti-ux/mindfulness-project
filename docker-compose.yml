services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: mindful
      POSTGRES_USER: mindful
      POSTGRES_PASSWORD: mindful
    ports:
      - "5432:5432"
    volumes:
      - mindful_postgres:/var/lib/postgresql/data

volumes:
  mindful_postgres:
